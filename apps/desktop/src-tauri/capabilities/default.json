{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions for the AIX desktop renderer",
  "windows": ["main"],
  "permissions": ["core:default"]
}
