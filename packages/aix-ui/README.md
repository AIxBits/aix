# @aix/ui

React renderer for validated AIX snapshots. It supports page, container, text, button, input, list, table, image, audio and video nodes. The package resolves bounded `$state` bindings and emits typed UI events; it has no connector, AI provider or Operation execution API.

Media nodes accept only resource handles supplied by the host. A Definition URL is never fetched directly by this package.
